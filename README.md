
  # ZooCare Mobile App UI (Copy)

  This is a code bundle for ZooCare Mobile App UI (Copy). The original project is available at https://www.figma.com/design/9BRXbG5EfzS2gDZ3iNL4As/ZooCare-Mobile-App-UI--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  